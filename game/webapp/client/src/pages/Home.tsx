// Style reminder: Home is the game shell; the first viewport should feel like a command screen, not a generic landing page.

import GameCanvas from "@/components/GameCanvas";

export default function Home() {
  return <GameCanvas />;
}
